[workspace]
members = [".", "openkit-macros"]

[package]
name = "openkit"
version = "0.1.2"
edition = "2024"
description = "A cross-platform CSS-styled UI framework for Rust"
license = "MIT OR Apache-2.0"
repository = "https://github.com/quinnjr/openkit"
homepage = "https://github.com/quinnjr/openkit"
documentation = "https://docs.rs/openkit"
readme = "README.md"
authors = ["Joseph R. Quinn"]
keywords = ["gui", "ui", "css", "cross-platform", "desktop"]
categories = ["gui", "rendering"]
rust-version = "1.85"

[features]
default = ["gpu", "macros"]
gpu = ["wgpu"]
macros = ["openkit-macros"]

[dependencies]
# ============================================================================
# OpenKit is a FULLY CUSTOM-RENDERED UI toolkit - NO GTK, NO Qt, NO native widgets
# All rendering is done via wgpu (GPU) or skia-rs (CPU) + softbuffer presentation
# Windowing uses winit which talks directly to X11/Wayland/Win32/Cocoa
# ============================================================================

# Proc macros (optional, enabled by default)
openkit-macros = { version = "0.1.2", path = "openkit-macros", optional = true }

# Windowing - pure Rust, talks directly to display servers (no GTK/Qt)
winit = "0.30"

# GPU Rendering (optional, enabled by default)
wgpu = { version = "23", optional = true }

# CPU Rendering (pure Rust Skia reimplementation)
skia-rs-safe = "0.1"

# Software buffer for CPU rendering presentation (X11 and Wayland native, no GTK)
softbuffer = { version = "0.4", features = ["x11", "x11-dlopen", "wayland", "wayland-dlopen"] }

# Alternative framebuffer for debugging
minifb = "0.27"

# Text rendering
cosmic-text = "0.12"

# CSS parsing
cssparser = "0.34"

# Image loading
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "webp"] }

# Color handling
palette = "0.7"

# Logging
log = "0.4"
env_logger = "0.11"

# Error handling
thiserror = "2"

# Async runtime for wgpu
pollster = "0.4"

# Raw window handle for wgpu integration
raw-window-handle = "0.6"

# Ordered float for CSS values
ordered-float = "4"

# Smallvec for performance
smallvec = "1"

# Unicode segmentation for text
unicode-segmentation = "1"

[dev-dependencies]
pretty_assertions = "1"

[[example]]
name = "hello_world"
path = "examples/hello_world.rs"

[[example]]
name = "theme_toggle"
path = "examples/theme_toggle.rs"

[[example]]
name = "angular_style"
path = "examples/angular_style.rs"

[[example]]
name = "custom_css"
path = "examples/custom_css.rs"

[[example]]
name = "softbuffer_test"
path = "examples/softbuffer_test.rs"
